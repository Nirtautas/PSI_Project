using Microsoft.AspNetCore.Mvc;

namespace TeamWebApplication.Controllers
{
    public class CourseEnvironmentController : Controller
    {
        /*
         * This controller is a placeholder
         * It should control the content displayed in an exact course
         * It should do so by fetching correct data from database (Of which code does not yet exist)
         */

        public IActionResult Index()
        {
            return View();
        }
    }
}
